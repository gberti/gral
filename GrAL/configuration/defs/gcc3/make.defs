# gcc/make.defs
# Settings for gcc 3.0.x, 3.1

CCC      =  gcc3 
CCFLAGS += -Wall
CCFLAGS += -DNMWR_INCLUDE_TEMPLATE_DEFS 
CCFLAGS += 
CCFLAGS += -ftemplate-depth-50
CCFLAGS += -DGRAL_HAS_SSTREAM
LDFLAGS += -Xlinker -rpath -Xlinker $(HOME)/lib
DEPEND    =$(CCC)
DEPFLAGS += -MM $(CCFLAGS)


